# --- Secrets / environment (doc 14 §9 — never commit real .env) ------------
.env
.env.*
!.env.example

# --- Dependencies ----------------------------------------------------------
node_modules/
.pnp
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# --- Build output (server dist, client build) ------------------------------
dist/
build/
out/
.vite/
*.tsbuildinfo

# --- Logs ------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Test / coverage -------------------------------------------------------
coverage/
.nyc_output/

# --- Caches ----------------------------------------------------------------
.cache/
.turbo/
.eslintcache
.parcel-cache/

# --- Local database / docker volumes (if ever bind-mounted locally) --------
pgdata/
redisdata/
*.sqlite
*.sqlite3

# --- Uploaded / generated media (durable storage is S3/R2, not the repo) ---
uploads/
tmp/

# --- Editor / OS -----------------------------------------------------------
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
# --- Design ------------------------------------------------------------------
FrontEnd Design/